interrupt vector handleing

peripheral firmware loading and configuration
    - Hardcoded indentifiers in firmware rom (eg. Storage device for os mounting)

file system
    - Custom simple file system 
    - Needs software (in C) to convert file to this FS and to raw data

file transfer via UART

floppy disk controller expansion card

SD Card controller expansion card

ESP32 Network expansion card
